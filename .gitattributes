/mvnw text eol=lf
*.cmd text eol=crlf
# Handle line endings consistently
* text=auto

# Treat all Java files as UTF-8
*.java text eol=lf

# Mark binary files so Git doesn't try to diff them
*.jar binary
*.war binary
*.zip binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary

# Make shell scripts always use LF
*.sh text eol=lf

# Make batch scripts always use CRLF
*.bat text eol=crlf
